---
description: "Invoke xforge-scaffold"
---

Use the `xforge-scaffold` Skill for this request. Begin with `xforge state` and resolve the Change ID, consume all Flow instructions dynamically, and distinguish Agent guidance from CLI/Gate evidence.
