---
description: "Invoke xforge-verify"
---

Use the `xforge-verify` Skill for this request. Begin with `xforge state` and resolve the Change ID, consume all Flow instructions dynamically, and distinguish Agent guidance from CLI/Gate evidence.
