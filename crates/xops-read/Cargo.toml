[package]
name = "xops-read"
description = "读模型：看板定义 · 表视图 · 单行历史 · 长文本原文。Web 唯一的查询契约。"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
xops-core.workspace = true
xops-store.workspace = true
xops-audit.workspace = true
xops-identity.workspace = true
xops-mcp.workspace = true
xops-table.workspace = true
# 个人看板要读 `_notices` 已经写下的行（NTF-001）。
# ⚠️ L3 → L1，**不成环**：xops-notice 不认识 xops-read。归属见 requirements/README §4。
xops-notice.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
# ⚠️ 这个 crate 以前一条集成测试都没有 —— 见 tests/read_model.rs 顶上那段。
# 两条依赖都已经在上面，这里是为了让 tests/ 直接 use 它们。
xops-audit.workspace = true
xops-notice.workspace = true

[lints]
workspace = true
